<!DOCTYPE html>
<html>
  <head>
    <title><%= current_tenant ? current_tenant.name : "Blog 6" %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <style type="text/css">
      :root { --background-color: #ddd; }
    </style>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div class="container">
      <div class="header"></div>
      <header>
        Header content
      </header>

      <nav>
        <% if current_user  %>
          Logged in: <%= current_user.email %>
        <% else %>
          Logged out
        <% end %>

        host: <%= request.host %><br>
        <% if current_tenant  %>
          tenant: <%= current_tenant.name %>
        <% else %>
          Unconfigured tenant
        <% end %>
      </nav>

      <main>
        <div class="grid-notice">Your browser does not support grid ... get a real browser</div>
        <div class="media-phone">Phone</div>
        <div class="media-tablet-portrait">Tablet Portrait</div>
        <div class="media-tablet-landscape">Tablet Landscape</div>
        <div class="media-desktop-small">Small Desktop</div>
        <div class="media-desktop">Desktop</div>
        <%= yield %>
      </main>

      <aside>
        <%= render 'blocks/tardis' %>
      </aside>

      <footer>
        <ul>
          <li><a href="#">Impressum</li>
          <li><a href="#">Impressum</li>
        </ul>
        <ul>
          <li><a href="#">Foodblog</li>
          <li><a href="#">Papas Blog</li>
          <li><a href="#">Thomas Bruderer</li>
          <li><a href="#">Apophis</li>
        </ul>
        <ul>
          <li><a href="#">Apophis</li>
        </ul>
        <ul>
          <li><a href="#">Apophis</li>
        </ul>
      </footer>
    </div>
  </body>
</html>
