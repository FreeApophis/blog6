<!DOCTYPE html>
<html>
  <head>
    <title><%= current_tenant ? current_tenant.name : "Blog 6" %></title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <style type="text/css">
      :root { --background-color: #ddd; }
    </style>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <div class="container">
      <div class="grid-notice">Your browser does not support grid ... get a real browser</div>
      <header>
        Header content
        <%= render 'blocks/tardis' %>
      </header>

      <nav>
        <% if current_user  %>
          Logged in: <%= current_user.email %>
        <% else %>
          Logged out
        <% end %>

        host: <%= request.host %><br>
        <% if current_tenant  %>
          tenant: <%= current_tenant.name %>
        <% else %>
          Unconfigured tenant
        <% end %>
      </nav>

      <main>
        <%= yield %>
      </main>

      <aside>
      </aside>

      <footer>
        Footer content
      </footer>
    </div>
  </body>
</html>
