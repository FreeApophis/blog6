<%= form_with(model: wedding_guest, local: true) do |form| %>
  <% if wedding_guest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wedding_guest.errors.count, "error") %> prohibited this wedding_guest from being saved:</h2>

      <ul>
        <% wedding_guest.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
