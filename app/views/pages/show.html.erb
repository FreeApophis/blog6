<h1><%= @page.title %></h1>

<%= @page.content %>

<% if @page.comments_allowed %>
  <%= render 'comments/comments', commentable: @page %>
  <%= render 'comments/comment_form', commentable: @page, comment: @page.comments.build %>
<% end %>
